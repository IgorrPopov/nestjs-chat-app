{
  "scripts": {
    "prod": "docker-compose up --build",
    "dev": "docker-compose -f docker-compose.yml -f docker-compose.development.yml up --build",
    "test": "docker-compose -f docker-compose.test.yml up --build",
    "test:e2e": "docker-compose -f docker-compose.test.e2e.yml up --build"
  }
}
